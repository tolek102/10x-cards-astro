---
import { Logo } from "../../Logo";

const features = [
  {
    title: "Szybkie tworzenie",
    description: "Twórz fiszki ręcznie lub generuj je automatycznie z pomocą AI",
    icon: "⚡",
    bgClass: "bg-gradient-to-br from-blue-50 to-indigo-50 border border-blue-100/20",
    iconClass: "bg-blue-500",
  },
  {
    title: "Efektywna nauka",
    description: "Wykorzystuj sprawdzone metody nauki z wykorzystaniem fiszek",
    icon: "📚",
    bgClass: "bg-gradient-to-br from-purple-50 to-pink-50 border border-purple-100/20",
    iconClass: "bg-purple-500",
  },
  {
    title: "Inteligentne powtórki",
    description: "System automatycznie planuje powtórki w optymalnych odstępach",
    icon: "🧠",
    bgClass: "bg-gradient-to-br from-emerald-50 to-teal-50 border border-emerald-100/20",
    iconClass: "bg-emerald-500",
  },
];
---

<section class="relative py-24 px-4 overflow-hidden bg-gradient-to-b from-white to-gray-50">
  <div class="absolute inset-0 bg-[radial-gradient(circle_500px_at_50%_200px,rgba(236,242,255,0.5),transparent)]"></div>
  
  <div class="relative max-w-4xl mx-auto text-center">
    <div class="mb-12">
      <Logo size="lg" withContainer client:load />
    </div>

    <div class="relative mb-16">
      <h1 class="text-6xl font-black tracking-tight mb-8 bg-clip-text text-transparent bg-gradient-to-r from-gray-900 via-gray-800 to-gray-600">
        Ucz się <span class="text-blue-600">10 razy szybciej</span><br/> 
        z inteligentnymi fiszkami
      </h1>
      <div class="absolute -bottom-6 left-0 right-0 h-[1px] bg-gradient-to-r from-transparent via-gray-200 to-transparent"></div>
    </div>

    <p class="text-xl text-gray-600 mb-20 leading-relaxed max-w-2xl mx-auto">
      Wykorzystaj moc sztucznej inteligencji do tworzenia i nauki z fiszkami. 
      Osiągaj lepsze wyniki w krótszym czasie.
    </p>
  </div>

  <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto px-4">
    {
      features.map((feature) => (
        <div class={`group relative rounded-2xl p-8 transition-all duration-300 hover:shadow-lg ${feature.bgClass}`}>
          <div class="flex justify-center mb-8">
            <div class={`w-16 h-16 flex items-center justify-center rounded-2xl text-3xl ${feature.iconClass} text-white shadow-lg transform transition-transform group-hover:scale-110 group-hover:rotate-3`}>
              {feature.icon}
            </div>
          </div>
          <h3 class="text-xl font-semibold mb-3 text-gray-900">{feature.title}</h3>
          <p class="text-gray-600 leading-relaxed">{feature.description}</p>
          <div class="absolute inset-0 border border-gray-100/20 rounded-2xl pointer-events-none"></div>
        </div>
      ))
    }
  </div>
</section> 